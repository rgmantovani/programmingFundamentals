#include<stdio.h>
int main() {
    // declarar variaveis
    int contador, numero, maior, menor;
    for(contador = 1; contador <= 10; contador = contador + 1){
        // solicitar um numero
        printf("Digite um numero inteiro positivo: ");
        // leitura do numero
        scanf("%i", &numero);
        /* Se contador for igual a 1:
            maior = numero
            menor = numero
           Senão
                Se o numero for menor que o menor:
                    menor recebe o numero
                Se o numero for maior que o maior:
                    maior recebe o numero
        */
    }
    // imprime o maior
    // imprime8 o menor
    
    return 0;
}












